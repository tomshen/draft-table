// Generated by CoffeeScript 1.6.3
var app, express, http, models, path;

express = require('express');

path = require('path');

http = require('http');

app = express();

models = require('./models.js');

models.School.create({
  name: 'name',
  email_domain: 'domain',
  plans: []
}, function(_id) {
  models.School.update(_id, {
    name: 'Penn'
  });
  models.School.addPlan(_id, {
    title: 'title',
    image_thumbnail: 'thumbnail',
    supporters: [],
    proposals: [],
    elements: []
  }, function(plan_id) {
    return console.log(plan_id);
  });
  models.School.get(_id, function(err, doc) {
    return console.log(doc);
  });
  return models.School.removePlanById('522ac1733c3dc5b41b000001', '522ac1733c3dc5b41b000002');
});

app.set('view engine', 'ejs');

app.set('views', __dirname + '/views');

app.configure(function() {
  app.set('port', process.env.PORT || 3000);
  return app.use('/public', express["static"](path.join(__dirname, 'public')));
});

app.get('/', function(req, res) {
  return res.render('index');
});

http.createServer(app).listen(app.get('port'));

console.log('Express server listening on port ' + app.get('port'));
